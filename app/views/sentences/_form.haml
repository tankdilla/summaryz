= simple_form_for([@article, @sentence], html: {class: 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :sentence_text, as: :text, input_html: { class: 'span5', rows: 5 }

  .form-actions
    = f.button :submit
